import type { TreeFoliageConfig } from './types';

export const DEFAULT_TREE_FOLIAGE_CONFIG: TreeFoliageConfig = {
  rulesVersion: 'tree-foliage-v3.0.0',
  minimumGeneration: 1,
  /*
   * ЛИСТЯ ПОЧИНАЄТЬСЯ З 0.22 ДОВЖИНИ ГІЛКИ, а було з 0.3.
   *
   * Три згустки розкладаються рівно до кінчика (`(slot+1)/slotCount`), тож за
   * порогу 0.3 вони сідали на 0.53, 0.77 і 1.0 — і нижня половина гілки
   * лишалась порожньою. Тут це помітив ЧУЖИЙ тест: «fills interior branch
   * spans» вимагає, щоб хоч один згусток був ближчим за половину довжини, і
   * впав. За 0.22 виходить 0.48, 0.74 і 1.0: гілка облистяна від низу до
   * кінчика.
   */
  terminalStart: 0.22,
  /*
   * Листя на стовбурі починається з 0.62 його довжини.
   *
   * Нижче — гола кора, як у справжнього дерева; вище — молодий приріст
   * останніх років, який у природі найлистяніший. До цього стовбур не діставав
   * листя ВЗАГАЛІ, і його верхня частина стояла жердиною, повз яку листя
   * росло тільки збоку.
   */
  trunkTerminalStart: 0.62,
  /*
   * СТЕЛЯ ЗГУСТКІВ 220, а було 64.
   *
   * 64 не мали жодного стосунку до вартості: згусток — це запис розміщення,
   * трикутники коштує ЛИСТЯ. Виміряно на живому дереві пари: 140 кандидатів,
   * випущено 43 — решту 97 відрізала стеля листя, і 32 гілки з 50 лишились
   * зовсім голими.
   */
  maxClusters: 220,
  /*
   * Стеля листя опущена з 900 до 700 (ADR-0072).
   *
   * Самоорганізаційна модель дала 56 гілок замість 13, і меш стовбура з
   * гілками подорожчав відповідно. Виміряно: контракт виходив на 18 883
   * трикутники проти мобільної стелі 18 000, а стовбур уже стискався
   * найгрубшим дозволеним кроком (4 з 2) і все одно не вкладався.
   *
   * Тут зменшено САМЕ листя, а не піднято стелю: стеля — опублікована межа
   * пристрою, і підганяти її під те, що не влізло, означало б скасувати
   * перевірку, а не пройти її. 700 листків на 57 згустків — це 12 на згусток,
   * тобто крона лишається щільною; заразом кожен листок дістає трохи більше
   * місця, бо гілок, на яких вони сидять, тепер учетверо більше.
   */
  maxLeaves: 900,
  /*
   * ТРИ-СІМ ЛИСТКІВ НА ЗГУСТОК, а було дванадцять-двадцять.
   *
   * Це те саме листя, розкладене інакше. За 12-20 бюджет у 700 листків
   * вичерпувався на 43-му згустку — виходили щільні кульки на вісімнадцяти
   * гілках і голі палиці на решті тридцяти двох. За 3-7 ті самі дев'ятсот
   * листків накривають усі 143 згустки: кожна гілка стає облистяною, а крона
   * читається кроною, а не помпонами (ADR-0075).
   *
   * Саме ЦЕ, а не листя на стовбурі, полагодило голу верхівку: найвищий
   * згусток піднявся з 3.82 на 4.28 при верхівці 4.44, бо листя нарешті
   * дійшло до наймолодших гілок.
   */
  minLeavesPerCluster: 2,
  maxLeavesPerCluster: 6,
  /*
   * КРОК МІЖ ЗГУСТКАМИ 0.34, СТЕЛЯ ВІСІМ НА ГІЛКУ.
   *
   * Кількість згустків мусить іти від ДОВЖИНИ гілки, а не лише від її ролі.
   * Виміряно на дереві пари: медіана довжини гілки 0.29, верхня чверть 0.56,
   * найдовша 4.14 — тобто розкид у ЧОТИРНАДЦЯТЬ разів. Три згустки, однакові
   * для всіх, робили короткій гілці щільний жмуток, а двометровій — три
   * плями з голим дротом між ними; саме такими й виглядали дві довгі бічні
   * гілки на знімку.
   *
   * Роль лишається НИЖНЬОЮ межею (`clustersByRole`), довжина може її лише
   * підняти. Розгортка кроку: 0.28 -> 149 згустків, 0.34 -> 139, 0.42 -> 129,
   * 0.50 -> 120. Взято 0.34: приблизно радіус згустка, тобто сусіди
   * стикаються, а не накладаються.
   */
  clusterSpacing: 0.34,
  maxClustersPerBranch: 8,
  minClusterRadius: 0.14,
  maxClusterRadius: 0.3,
  clustersByRole: {
    trunk: 3,
    primary: 2,
    secondary: 3,
    twig: 3,
  },
};
