import type { ReefFoundationMeshConfig } from './types';

export const DEFAULT_REEF_FOUNDATION_MESH_CONFIG: ReefFoundationMeshConfig = {
  rulesVersion: 'reef-foundation-mesh-v1.0.0',
  skirtDepthRatio: 0.18,
  maximumVertices: 256,
  maximumTriangles: 512,
};
