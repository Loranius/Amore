import type { TreeLeafGeometryConfig } from './types';

export const DEFAULT_TREE_LEAF_GEOMETRY_CONFIG: TreeLeafGeometryConfig = {
  rulesVersion: 'tree-leaf-geometry-v1.3.0',
  renderFractionByLod: {
    high: 0.8,
    medium: 0.76,
    low: 0.34,
  },
  /*
   * СТЕЛЯ ЛИСТЯ НА ТЕЛЕФОНІ ОПУЩЕНА З 720 ДО 660.
   *
   * Це закриває ЧУЖУ ваду, знайдену в ADR-0093 §6: сорокарічна пара, що жила
   * всіма модулями, вибивала мобільну стелю трикутників ще до появи
   * скелетних гілок — 19 591 при 18 000. Тутешні тести її не бачили, бо
   * перевіряли інші віки.
   *
   * Стовбур віддати вже не міг: у найгіршому дереві він стоїть на
   * найгрубішому дозволеному кроці 8 і важить 12 262 трикутники. Корені й
   * дрібнота на землі мають власні стелі. Лишалось листя.
   *
   * Виміряно на 9 віках × 4 посіви активної пари, `medium`, після того як
   * пластинка листка стала шестикутною замість восьмикутної:
   *
   *   стеля 720 -> найгірше 18 338, порушень 1 з 36
   *   стеля 690 -> найгірше 18 158, порушень 1 з 36
   *   стеля 660 -> найгірше 17 978, порушень 0 з 36   <-
   *   стеля 640 -> найгірше 17 858, порушень 0 з 36
   *
   * 660 — найбільша стеля, за якої обіцянка телефону тримається. Дешевшати
   * далі нема сенсу: 640 не купує нічого, крім шістдесяти листків менше.
   *
   * `high` лишається 720: там стеля 24 000 і найгірше дерево дає 23 824.
   */
  maxInstancesByLod: {
    high: 720,
    medium: 660,
    low: 360,
  },
  minimumLength: 0.14,
  maximumLength: 0.32,
  minimumWidthRatio: 0.42,
  maximumWidthRatio: 0.62,
  radialSpread: 0.76,
  axialSpread: 0.58,
};
