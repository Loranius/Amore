import type { TreeMaterialConfig } from './types';

export const DEFAULT_TREE_MATERIAL_CONFIG: TreeMaterialConfig = {
  rulesVersion: 'tree-material-v1.1.0',
  quantizationSteps: 16,
  maxMaterials: 2,
};
