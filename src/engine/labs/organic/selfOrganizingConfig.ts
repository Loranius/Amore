// ============================================================
// Числа самоорганізаційного росту — і чим кожне з них виміряне.
// ------------------------------------------------------------
// Жодне значення тут не вибране на око. Кожне взяте з розгортки по живій
// моделі, і поруч записано, ЩО САМЕ воно рухає й у яких межах лишається
// осмисленим. Там, де параметр не робить нічого, так і сказано — це
// корисніше за правдоподібне число.
//
// Три величини, якими міряється результат:
//
//   СТРУНКІСТЬ — висота, поділена на діаметр основи. У справжніх дерев
//   20-60; у старої моделі було 9.2, тобто стовбур-довбня.
//   ЧИСТА ВИСОТА СТОВБУРА — на якій частці висоти сидить найнижча гілка.
//   У старої моделі 55%: жердина з мітлою нагорі. Кущ — це 3%.
//   ГІЛОК — скільки їх узагалі. У старої моделі 13 із жорсткою стелею
//   в три покоління.
// ============================================================
import type { SelfOrganizingConfig } from './selfOrganizing';

export const DEFAULT_SELF_ORGANIZING_CONFIG: SelfOrganizingConfig = {
  rulesVersion: 'tree-self-organizing-v1.0.0',

  /*
   * ЦИКЛИ Й СИЛА — це те, чим історія пари стає деревом, тож обидва числа
   * тут лише запасні: справжні підставляє порода (`treeToOrganicField`).
   * Один цикл — один рік стосунків.
   */
  cycles: 4,
  vigourPerCycle: 12,

  /*
   * ТІНЬОВА СІТКА.
   *
   * `cellSizeRatio` у ДОВЖИНАХ МІЖВУЗЛЯ, а не в одиницях сцени — без цього
   * `internodeLength` переставав бути розміром (див. `selfOrganizing.ts`).
   *
   * `shadowDepth` — 7, і чесно кажучи, це число НІЧОГО НЕ ВИРІШУЄ. Виміряно:
   * за спаду 1.05 глибини 7, 9, 11 і 14 дають ЦІЛКОМ однакове дерево — до
   * вузла. Далі сьомої комірки внесок уже накопичився до насичення. Лишено
   * найменшим із тих, що дають той самий результат, бо вартість росте як
   * куб глибини: розгортка з глибиною 20 йшла 17 секунд замість двох.
   */
  cellSizeRatio: 1.2,
  shadowStrength: 0.55,
  shadowDecay: 1.05,
  shadowDepth: 7,

  /*
   * ВЕРХІВКОВЕ ПАНУВАННЯ — одне число, що відрізняє тополю від яблуні.
   * Виміряно на восьмициклічному дереві:
   *
   *   λ=0.50 -> висота 12.6, ширина 7.8, гілок 37   (розлоге)
   *   λ=0.62 -> висота 15.1, ширина 6.3, гілок 28
   *   λ=0.85 -> висота 23.6, ширина 8.3, гілок 17   (колоноподібне)
   *
   * 0.62 — дерево, що тягнеться вгору, але не в стовп.
   */
  apicalControl: 0.62,
  minimumVigour: 0.9,

  /*
   * СКИДАННЯ ГІЛОК — те, що робить стовбур стовбуром, і єдиний важіль, яким
   * чиста висота стовбура взагалі керується. Виміряно (терпіння -> висота
   * найнижчої гілки / гілок / стрункість):
   *
   *   0.3 -> 59% / 58 гілок / 65.7
   *   0.5 -> 48% / 70 гілок / 60.4
   *   0.7 -> 25% / 96 гілок / 52.3
   *   0.9 ->  4% / 131 гілка / 45.4
   *
   * Рівно монотонно, і саме тому взято 0.7: 25% — це дерево з видимим
   * стовбуром, а не кущ (4%) і не жердина (59%).
   *
   * До накопичення голоду ця ж величина була ОБРИВОМ: зміна ребра комірки з
   * 0.400 на 0.408 перекидала висоту з 18% на 76%. Тобто монотонність тут —
   * не властивість параметра, а наслідок того, що голод згладжено в часі.
   */
  sheddingLight: 0.15,
  sheddingGraceCycles: 2,
  sheddingTolerance: 0.7,

  /*
   * РОЗМІР. `internodeLength` — чистий масштаб: виміряно, що за 0.10, 0.14,
   * 0.18, 0.24 і 0.34 виходить ОДНЕ Й ТЕ САМЕ дерево (141 вузол, 31 гілка,
   * ті самі покоління), лише більше або менше. Це й було метою прив'язки
   * тіньової сітки до міжвузля.
   *
   * 0.11 обрано так, щоб доросле дерево пари лишалось у тому ж габариті, у
   * якому вже стоять камера, ґрунт і корені.
   */
  internodeLength: 0.203,
  /*
   * КУТ ВІДХОДУ БІЧНОЇ ГІЛКИ — і разом із нахилом гілок він вирішує, чи буде
   * крона розлогою, чи йоржиком. Виміряно відношення ширини до висоти:
   *
   *   кут 0.6 -> 0.85     світло 0.10 -> 0.74     гілки↓ 0.05 -> 1.01
   *   кут 0.9 -> 1.01     світло 0.25 -> 0.80     гілки↓ 0.15 -> 0.90
   *   кут 1.1 -> 0.96     світло 0.42 -> 0.96     гілки↓ 0.28 -> 0.98
   *   кут 1.3 -> 0.80     світло 0.60 -> 1.06
   *
   * До того ж дерево було 0.44 — вузька колона листя вздовж стовбура.
   * Причиною була не жодна з цих трьох величин, а те, що порожній градієнт
   * тіні віддавав «угору» (див. `brightestDirection`).
   */
  branchAngleRad: 1.1,
  directionWeight: 0.62,
  lightWeight: 0.42,
  /** Тяжіння вниз: гілка, що пішла вбік, поволі хилиться, як і має. */
  /*
   * НАХИЛ БІЧНИХ ГІЛОК. Виміряно похилом гілки — синусом кута від її основи
   * до кінця; додатний означає «гілка йде вгору», від'ємний — плакуча верба:
   *
   *   +0.05 -> похил +0.624      -0.12 -> похил -0.058
   *    0.00 -> похил +0.544      -0.20 -> похил -0.546
   *   -0.05 -> похил +0.301      -0.28 -> похил -0.583
   *
   * Спершу тут стояло -0.28, і на знімку виросла ПЛАКУЧА ВЕРБА: гілки
   * звисали до самої землі. Числа при цьому казали, що все гаразд —
   * відношення ширини до висоти було 0.98, тобто «розлога крона». Ширина не
   * відрізняє розлогого дерева від плакучого: обидва широкі. Тому й
   * заведено похил — метрика, яка бачить саме те, що видно оком.
   */
  tropism: { x: 0, y: -0.05, z: 0 },
  /*
   * ВУЗЛУВАТІСТЬ — і саме вона виявилась причиною нахилу.
   *
   * На знімку дерево стояло зігнуте вбік із голою лівою половиною, і я шукав
   * причину в тропізмі та в світлі. Причина була в тому, що цей параметр був
   * ПРИБИТИЙ ДО СТЕЛІ: він ділився на радіус кінчика (0.014), давав 2 радіани
   * й упирався в запобіжник 0.6 — тобто кожне міжвузля довертало пагін на 34°
   * у випадковий бік, і крону просто зносило.
   *
   * Виміряно після виправлення (нахил вершини / зсув крони, у частках):
   *
   *   0.05 -> 0.021 / 0.060      0.32 -> 0.034 / 0.213
   *   0.12 -> 0.027 / 0.034      0.45 -> 0.051 / 0.282
   *   0.20 -> 0.033 / 0.093
   *
   * Проти 0.202 / 0.145 до виправлення. 0.12 — найрівніше з усього ряду, і
   * при цьому дерево не стає циркульною дугою.
   */
  gnarliness: 0.12,
  /** Верхівка йде проти тяжіння — саме цим стовбур і стоїть прямо. */
  apicalTropism: { x: 0, y: 0.3, z: 0 },

  /*
   * ТОВЩИНА. Обидва числа міняють САМЕ товщину й нічого більше — виміряно:
   * за різних `tipRadius` і `pipeExponent` виходить дерево з тим самим
   * числом вузлів, гілок і поколінь, з тією ж висотою й шириною.
   *
   *   tipRadius 0.010 -> стрункість 41.3      pipeExponent 1.8 -> 56.5
   *   tipRadius 0.014 -> стрункість 29.5      pipeExponent 2.2 -> 81.8
   *   tipRadius 0.024 -> стрункість 17.2      pipeExponent 3.0 -> 127.6
   *
   * (перші — на живому дереві пари, другі — на восьмициклічному зразку)
   *
   * 2.2 — класичний показник трубкової моделі для дерев (да Вінчі дає 2.0,
   * виміри на живих деревах — 2.0..2.5).
   */
  tipRadius: 0.014,
  pipeExponent: 2.2,

  /*
   * СТЕЛІ. Це запобіжники, а не форма: за нинішніх чисел доросле дерево
   * пари дає близько 400 вузлів, тож 900 — вдвічі з гаком запасу. Стеля
   * поколінь так само не в'яже: модель сама зупиняється на 3-5.
   */
  maxNodes: 900,
  maxGeneration: 5,
};
