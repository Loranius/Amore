// ============================================================
// BloomProbe — повноекранний Bloom. ТІЛЬКИ ДІАГНОСТИКА (`?gfx=bloom`).
// ------------------------------------------------------------
// Другий із двох підозрюваних у білому фоні на пристрої власника (перший —
// карта оточення, render/envMap.ts). У липні їх прибрали ОДНИМ коммітом
// (`ba6ad77`), тож жоден не перевірений поодинці — а `EffectComposer`
// рендерить сцену у власні таргети й потім блітить, тобто саме той клас
// коду, де alpha на мобільних драйверах ламається найчастіше.
//
// Окремим файлом і за `React.lazy` — навмисно: `postprocessing` важить
// сотні кілобайт, і поки Bloom не в дефолті, він не має лежати в головному
// чанку кожної пари. Динамічний імпорт лишає його окремим чанком, який
// вантажиться лише коли в URL є прапорець.
// ============================================================
import { Bloom, EffectComposer } from '@react-three/postprocessing';

export default function BloomProbe() {
  return (
    <EffectComposer>
      {/*
        ПОРІГ ПІДІБРАНО ПІСЛЯ ТОГО, ЯК ПРИСТРІЙ СКАЗАВ СВОЄ (ADR-0173).
        Тут стояло «значення підбирати після того, як пристрій скаже, чи
        Bloom узагалі дозволений» — 9 вересня він сказав.

        0.72 → 0.92, бо денне небо порталу саме по собі яскраве:
        виміряно 235 з 255 у теплій імлі над горизонтом. При 0.72
        світилось УСЕ світле — хмари ставали білими плитами, мармур храму
        жовтів, і пересвічене у вікні кристала стрибнуло з 1.18% до
        8.28%. Bloom має світити артефакт, а не небо за ним.

        Сила 0.6 → 0.45 із тієї ж причини: у сцені, де світле тло, кожна
        десята сили коштує пересвіченого неба.

        ЩО ПЕРЕВІРЕНО Й НЕ СПРАЦЮВАЛО. Вище 0.92 поріг перестає щось
        міняти взагалі: 0.92 і 0.97 дали однакові числа до пікселя
        (смуга хмар 217, пересвічено на кристалі 2.99%). Тобто яскраве в
        цій сцені приходить у Bloom уже понад одиницю, і відрізати небо
        від артефакта самим порогом не можна.
        
        Додавання `ToneMapping` у ланцюг — теж перевірено й теж гірше
        (хмари 221, пересвічено 6.60%): крива вже застосовується, і
        друга робить кадр яскравішим, а не чистішим. Записано, щоб ніхто
        не пробував це вдруге.
      */}
      <Bloom intensity={0.45} luminanceThreshold={0.92} luminanceSmoothing={0.2} mipmapBlur />
    </EffectComposer>
  );
}
