// ============================================================
// Заглушка на час завантаження 3D-сцени.
// ------------------------------------------------------------
// Досі фолбеком <Suspense> у HomePage стояв Crystal.tsx — SVG-кристал
// ПЕРШОЇ версії. Через це кожен холодний вхід на головну показував
// восьмипелюсткове рожеве віяло, потім воно розквітало різнокольоровими
// гранями (щойно приходили дані), і аж тоді все це підмінялось
// артефактом. Три різні кристали поспіль, два з яких давно не є
// дизайном застосунку.
//
// Причина, чому вікно велике, а не мить: чанк CrystalScene важить
// близько 900 КБ (three.js + drei). На телефоні в дорозі це секунди.
//
// Crystal.tsx лишається фолбеком там, де він і мусить бути: коли WebGL
// недоступний або сцена ВПАЛА (CrystalErrorBoundary). Там показати
// щось замість нічого — правильно. А «ще качається» — не той випадок:
// сцена ось-ось буде, і підміняти її іншим кристалом означає збрехати
// про те, який вигляд має застосунок.
//
// Форма заглушки навмисно не кристал: м'яке світло в тій самій рамці.
// Воно нічого не обіцяє й ні з чим не свариться, коли на його місце
// стає справжній артефакт.
// ============================================================
import { CrystalStats } from './CrystalStats';
import { EMPTY_DELTAS, EMPTY_DNA } from './useCrystal';

export function CrystalPlaceholder() {
  return (
    <>
      {/* Той самий .crystal-wrap, що й у сцени та у SVG-фолбека: рамка
          задає розмір, тож підміна не зсуває нічого нижче. */}
      <div className="crystal-wrap crystal-wrap--loading" aria-hidden="true">
        <span className="crystal-loading-glow" />
      </div>
      {/* Смужка статистики теж на місці й у стані очікування — інакше
          вона з'явилася б аж із 3D-сценою й підстрибнула б уся сторінка. */}
      <CrystalStats dna={EMPTY_DNA} deltas={EMPTY_DELTAS} isPending />
    </>
  );
}
