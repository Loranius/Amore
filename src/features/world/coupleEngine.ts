// ============================================================
// Дві сталі пари: версія рушія і її часовий пояс.
// ------------------------------------------------------------
// Окремий файл на два рядки — і причина в тому, звідки вони поїхали.
// Лежали вони в `portalSources.ts`, який тягне за собою клієнт Supabase;
// отже будь-хто, кому потрібен був лише ЧАСОВИЙ ПОЯС, отримував разом із
// ним мережевий клієнт. `yearFills.ts` — чиста функція над знімком — саме
// так і опинилась залежною від бази: її тест падав на відсутньому
// `.env.local`, хоча жодного запиту не робив.
//
// Стала не має права тягти за собою клієнта. Тепер і не тягне.
// ============================================================

export const ENGINE_VERSION = '1.0.0';
export const COUPLE_TIME_ZONE = 'Europe/Kyiv';
