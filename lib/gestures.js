// ============================================================
// GESTURES — pull-to-refresh вимкнено
// ============================================================
const Gestures = (() => {
  function init() {
    // pull-to-refresh прибрано
  }
  return { init };
})();

window.Gestures = Gestures;
